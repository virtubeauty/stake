{
  "name": "vbea_staking_fe",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run generate && next dev --turbopack",
    "dev:testnet": "cross-env NETWORK=testnet npm run generate && next dev --turbopack",
    "dev:mainnet": "cross-env NETWORK=mainnet npm run generate && next dev --turbopack",
    "build": "npm run generate && next build",
    "build:testnet": "cross-env NETWORK=testnet npm run generate && next build",
    "build:mainnet": "cross-env NETWORK=mainnet npm run generate && next build",
    "start": "next start",
    "lint": "next lint",
    "generate": "wagmi generate"
  },
  "dependencies": {
    "@mantine/core": "^8.0.0",
    "@mantine/notifications": "^8.0.0",
    "@rainbow-me/rainbowkit": "^2.2.4",
    "@tanstack/react-query": "^5.75.5",
    "dotenv": "^16.5.0",
    "next": "15.3.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "viem": "^2.29.0",
    "wagmi": "^2.15.2"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@wagmi/cli": "^2.3.0",
    "cross-env": "^7.0.3",
    "eslint": "^9",
    "eslint-config-next": "15.3.2",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
